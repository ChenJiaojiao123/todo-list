import React, { Component } from 'react';
import {observer} from 'mobx-react';

@observer
class Mobx extends Component {
  render() {
    return (
      <div>
       hell mobx
      </div>
    );
  }
}

export default Mobx;
